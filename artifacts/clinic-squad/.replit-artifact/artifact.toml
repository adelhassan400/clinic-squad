kind = "web"
previewPath = "/"
title = "ClinicSquad"
version = "1.0.0"
id = "artifacts/clinic-squad"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20985

[services.development]
run = "pnpm --filter @workspace/clinic-squad run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clinic-squad", "run", "build" ]
publicDir = "artifacts/clinic-squad/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20985"
BASE_PATH = "/"
